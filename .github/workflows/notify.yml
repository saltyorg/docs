name: Notify

on:
  workflow_run:
    workflows: ["Build & Deploy", "Build PR & Deploy"]
    types:
      - completed

permissions:
  actions: read
  contents: read
  pull-requests: read

jobs:
  notify:
    if: contains(fromJSON('["success", "failure", "timed_out", "startup_failure", "cancelled"]'), github.event.workflow_run.conclusion)
    runs-on: ubuntu-24.04
    steps:
      - name: Notify workflow completion
        uses: saltyorg/github-actions/notify@cbf157255aff0ff6fc4738b11ee595646455e55a # v1.1.0
        with:
          github-token: ${{ github.token }}
          discord-webhook: ${{ secrets.DISCORD_WEBHOOK }}
